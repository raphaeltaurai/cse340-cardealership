<% if (title) { %>
    <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
  } %>
  
  <%- messages() %>
  <div class="management-links" style="margin:2rem 0;display:flex;gap:1.5rem;flex-wrap:wrap;">
    <a href="/inv/add-classification" class="btn-main">Add New Classification</a>
    <a href="/inv/add-inventory" class="btn-main">Add New Inventory</a>
  </div>