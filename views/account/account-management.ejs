<% if (title) { %>
    <h1><%= title %></h1>
  <% } %>
  
  <% if (typeof errors !== 'undefined' && errors.errors && errors.errors.length > 0) { %>
    <div class="notice">
      <ul>
        <% errors.errors.forEach(function(error) { %>
          <li><%= error.msg %></li>
        <% }) %>
      </ul>
    </div>
  <% } %>
  <%- messages() %>

  <% if (accountData) { %>
    <h2>Welcome <%= accountData.account_firstname %></h2>
    <p><a href="/account/update/<%= accountData.account_id %>">Update Account Information</a></p>
    <% if (accountData.account_type === 'Employee' || accountData.account_type === 'Admin') { %>
      <h3>Inventory Management</h3>
      <p><a href="/inv/">Go to Inventory Management</a></p>
      <h3>Order Inbox</h3>
      <p><a href="/offers/inbox">View New Offers Inbox</a></p>
    <% } %>
  <% } %>
